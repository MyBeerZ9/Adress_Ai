# AddressAI Root Environment Configuration

# Backend API Configuration
# Optional: Google Maps Geocoding API Key (falls back to OpenStreetMap / Heuristics if omitted)
GOOGLE_API_KEY=your_google_maps_api_key_here

# Backend Server Host & Port
HOST=0.0.0.0
PORT=8000
DEBUG=True

# Frontend Configuration (Optional: set in production if backend is hosted separately)
# VITE_API_BASE_URL=https://your-backend-url.onrender.com
